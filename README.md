[![.NET Core CI](https://github.com/Stw1k/RemoteDriveClient/actions/workflows/main.yml/badge.svg?branch=master)](https://github.com/Stw1k/RemoteDriveClient/actions/workflows/main.yml)
Під час виконання цієї лабораторної роботи я налаштував автоматичне тестування (CI) для проєкту за допомогою GitHub Actions. Ось деякі ключові рішення та проблеми, з якими я зіткнувся.

🖥️ Чому тести запускаються лише на Windows?
У завданні стояла вимога запустити тести на матриці ОС (Windows, Linux, macOS). Однак на ранніх етапах збірка "падала" на Linux та macOS.

Причина: Основний проєкт (RemoteDriveClient) є додатком Windows Forms, який побудований на базі .NET Framework 4.8.

.NET Framework — це технологія, яка працює виключно у Windows.

Віртуальні машини ubuntu-latest (Linux) та macos-latest (macOS) не можуть встановити .NET Framework і, відповідно, не можуть зібрати (скомпілювати) проєкт.

Через це було прийнято рішення обмежити матрицю тестування лише операційною системою os: [windows-latest], оскільки це єдина платформа, на якій проєкт може бути зібраний та протестований.

🐞 Проблеми під час налаштування
Навіть після переходу лише на Windows, я зіткнувся з двома основними проблемами:

Конфлікт архітектури (x86 vs. Any CPU):

Проблема: Проєкт WinForms вимагав збірки під 32-бітну архітектуру (x86). За замовчуванням GitHub Actions намагався зібрати його під Any CPU (64-біт), що призводило до помилок збірки.

Рішення: Я додав прапорець -p:Platform=x86 до всіх команд збірки та тестування у файлі main.yml.

Відновлення NuGet-пакетів (packages.config):

Проблема: Навіть із правильним прапором x86, збірка не могла знайти ключові залежності, наприклад Newtonsoft.Json (Error: CS0246). Це сталося тому, що сучасна команда dotnet restore некоректно працювала зі старим форматом файлу packages.config в поєднанні з примусовою платформою x86.

Рішення: Замість dotnet restore, я додав кроки для завантаження оригінального клієнта nuget.exe та виконав команду ./nuget.exe restore. Це надійно відновило всі пакети, і наступний крок збірки зміг їх знайти.
