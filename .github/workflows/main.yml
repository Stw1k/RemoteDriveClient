name: .NET Core CI


on:
  push: 
    branches: [ "master" ]
  pull_request: 
    branches: [ "master" ]

jobs:
  build-and-test:
    
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest] 

    steps:
    
    - name: Checkout code
      uses: actions/checkout@v4

    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x' 

    
    - name: Restore packages (MSBuild)
      run: dotnet msbuild RemoteDriveClient.sln -t:Restore -p:Platform=x86

    - name: Build
      run: dotnet build RemoteDriveClient.sln --no-restore -p:Platform=x86

    - name: Test
      run: dotnet test RemoteDriveClient.sln --no-build --verbosity normal -p:Platform=x86
